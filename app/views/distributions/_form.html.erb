<%= form_with(model: distribution) do |form| %>
  <% if distribution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(distribution.errors.count, "error") %> prohibited this distribution from being saved:</h2>

      <ul>
        <% distribution.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label "何個作りますか？" %>
    <%= form.number_field :id, max: 100, required: true, autofocus: true %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
